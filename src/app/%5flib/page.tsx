export default function PrivateRoute() {
    return <h1>You cannot view this in Browser</h1>;
    // in this folder %5f is used to represent _ in our url if we really want to use it if we directly use _ next makes that folder private
}
